This folder is used by pinecone-openai-load.py for video embedding tests.

A real network download is blocked in this environment, so a placeholder file is included.
Replace `sample-video-placeholder.avi` with real `.mp4/.mov/.mkv/.webm/.avi` files.
